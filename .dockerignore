# Secrets must never reach an image layer - they are passed at run time instead.
.env
*.env
!.env.example
tipbot.yaml

# Build output and local state; the image builds its own.
build/
.gradle/
.kotlin/
*.db
*.db-journal

.git/
.idea/
.vscode/

# The marketing site is served by the caddy image straight from a bind mount. It is not part
# of the bot image and only slows the build context down if it is sent.
web/
